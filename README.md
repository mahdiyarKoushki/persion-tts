تبدیل متن به گفتار فارسی با استفاده از مایکروسافت اج (edge-tts)

این پروژه یک راهکار ساده و قدرتمند برای تبدیل متن به گفتار (Text-to-Speech) فارسی با لهجه ایرانی کاملاً طبیعی است. این سیستم بر پایه سرویس رایگان Microsoft Edge TTS (صداهای مدرن Neural) طراحی شده است.

✨ ویژگی‌ها

🎙️ صدای بسیار طبیعی: بهره‌گیری از تکنولوژی Neural مایکروسافت (صدای فرید برای مرد و دیلارا برای زن) با وضوح بالا.

💸 کاملاً رایگان: بدون نیاز به ثبت‌نام، ایجاد حساب کاربری یا دریافت کلید اختصاصی (API Key).

🇮🇷 پشتیبانی کامل از فارسی: بهینه‌شده برای زبان فارسی با لهجه استاندارد ایران (fa-IR).

🛠️ پشتیبانی از SSML: امکان کنترل سرعت (Speed)، زیر و بمی صدا (Pitch) و ایجاد مکث (Pause) در متن.

💻 چندسکویی: قابل اجرا بر روی تمامی سیستم‌عامل‌های ویندوز، مک و لینوکس.

🚀 کاربردی: ایده‌آل برای تولید پادکست، ویدیوهای آموزشی، کتاب‌های صوتی و ربات‌های تلگرام یا دیسکورد.

📋 پیش‌نیازها

پیش از شروع، مطمئن شوید موارد زیر بر روی سیستم شما نصب شده باشند:

پایتون ۳.۸ یا بالاتر (نسخه پیشنهادی: ۳.۱۱ یا ۳.۱۲)

اتصال به اینترنت (جهت ارتباط با سرورهای مایکروسافت)

مدیریت بسته PIP (برای نصب کتابخانه‌های پایتون)

⚙️ نصب وابستگی‌ها (Installation)

برای نصب کتابخانه اصلی پروژه، دستور زیر را در ترمینال یا CMD خود وارد کنید:

pip install edge-tts


نکته: اگر در ویندوز با خطای SSL مواجه شدید، مطمئن شوید که گواهی‌های امنیتی سیستم‌عامل شما به‌روز هستند.

🎧 صداهای پیش‌فرض (Voices)

در حال حاضر دو صدای باکیفیت برای زبان فارسی توسط مایکروسافت ارائه شده است:

نام صدا

جنسیت

شناسه (ShortName)

فرید

مرد

fa-IR-FaridNeural

دیلارا

زن

fa-IR-DilaraNeural

🚀 راهنمای استفاده (Usage)

۱. استفاده از خط فرمان (CLI)

سریع‌ترین راه برای تولید یک فایل صوتی از متن:

edge-tts --voice fa-IR-FaridNeural --text "سلام، به این پروژه خوش آمدید" --write-media output.mp3


۲. استفاده در کدهای پایتون

شما می‌توانید این سرویس را به‌راحتی در پروژه‌های خود ادغام کنید:

import edge_tts
import asyncio

# متن مورد نظر برای تبدیل به صدا
TEXT = "این یک مثال از تبدیل متن به گفتار فارسی با کیفیت بالا است."
# انتخاب صدا
VOICE = "fa-IR-DilaraNeural"
# نام فایل خروجی
OUTPUT_FILE = "output.mp3"

async def generate_audio():
    communicate = edge_tts.Communicate(TEXT, VOICE)
    await communicate.save(OUTPUT_FILE)
    print(f"فایل صوتی با موفقیت در '{OUTPUT_FILE}' ذخیره شد.")

if __name__ == "__main__":
    asyncio.run(generate_audio())


🛠️ عیب‌یابی (Troubleshooting)

خطای SSL: اگر در ویندوز با خطای گواهی امنیتی روبرو شدید، ابتدا PIP را ارتقا دهید: python -m pip install --upgrade pip.

کندی در پردازش: از آنجایی که تبدیل متن در سرورهای ابری انجام می‌شود، سرعت اینترنت شما در زمان ساخت فایل مؤثر است.

کنترل دقیق‌تر گفتار: برای داشتن خروجی حرفه‌ای‌تر، می‌توانید از تگ‌های SSML استفاده کنید تا لحن و سرعت خواندن متن را شخصی‌سازی کنید.

🤝 مشارکت و حمایت

استفاده از این پروژه برای مقاصد شخصی محدودیتی ندارد. اگر پیشنهادی برای بهبود کدها یا افزودن قابلیت‌های جدید دارید، خوشحال می‌شویم در توسعه آن مشارکت کنید.
